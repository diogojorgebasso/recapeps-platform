export default function DashboardMock() {
    return (
        <div>
            <h1>Dashboard</h1>
            <p>
                This is a mock dashboard page. It doesn't have any real functionality, but it's here to showcase the routing and authentication setup.
            </p>
        </div>
    );
}